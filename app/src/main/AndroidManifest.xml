<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bancard.vpos_pagomovil_android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".payment.view.PaymentActivity"
            android:label="Pago">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".vpos.view.VPOSActivity"
            android:noHistory="true"
            android:label="Ingrese los datos de la tarjeta"/>
        <activity
            android:name=".confirmation.view.ConfirmationActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:label="Confirmación"/>
    </application>

</manifest>